import { RaphaelDirective } from './raphael.directive';

describe('RaphaelDirective', () => {
  it('should create an instance', () => {
    const directive = new RaphaelDirective();
    expect(directive).toBeTruthy();
  });
});
